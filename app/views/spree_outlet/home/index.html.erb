<script>
  var user, order, Spree = Spree || {};
  <% if @user %>
  Spree.api_key = "<%= @user.spree_api_key %>";
  // FIXME. SHOULD BE API FOR THIS.
  user = {
    user_id: "<%= @user.id %>",
    api_key: "<%= @user.spree_api_key %>",
    email: "<%= @user.email %>"
  };
  <% end %>
  <% if @order %>
  order = <%= render(:file => 'spree/api/orders/show').html_safe %>
  <% end %>
</script>
